import { EstadosPedidoPipe } from './estados-pedido.pipe';

describe('EstadosPedidoPipe', () => {
  it('create an instance', () => {
    const pipe = new EstadosPedidoPipe();
    expect(pipe).toBeTruthy();
  });
});
